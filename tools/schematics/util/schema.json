{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsUtil",
  "title": "Util Schematic",
  "type": "object",
  "description": "Generate a single utility library within a domain or shared folder",
  "properties": {
    "app": {
      "type": "string",
      "default": "app",
      "description": "Target app name (e.g., app)",
      "x-prompt": "What is the target app name?"
    },
    "domain": {
      "type": "string",
      "description": "Domain name (e.g., products, users). Not required when --shared is used."
    },
    "name": {
      "type": "string",
      "description": "Utility name (e.g., formatters, validators, helpers)",
      "x-prompt": "What is the utility name?"
    },
    "shared": {
      "type": "boolean",
      "description": "Whether to place the utility in the shared folder"
    },
    "help": {
      "type": "boolean",
      "description": "Show help information",
      "default": false
    }
  },
  "required": ["app", "name"]
}
