# <%= classify(domain) %> Types

This library contains TypeScript interfaces and types for the <%= domain %> domain.

## Usage

Import types in your components and services:

```typescript
import { <%= classify(domain) %> } from '@<%= app %>/<%= domain %>/types';
```

## Structure

- `src/lib/<%= domain %>.models.ts` - Domain models and interfaces
- `src/public-api.ts` - Public API exports
