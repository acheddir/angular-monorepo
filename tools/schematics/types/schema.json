{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsTypes",
  "title": "Types Schematic",
  "type": "object",
  "description": "Generate a single types library within a domain or shared folder",
  "properties": {
    "app": {
      "type": "string",
      "description": "Target app name (e.g., app)",
      "x-prompt": "What is the target app name?"
    },
    "domain": {
      "type": "string",
      "description": "Domain name (e.g., products, users). Not required when --shared is used."
    },
    "shared": {
      "type": "boolean",
      "description": "Whether to place the types library in the shared folder",
      "default": false
    }
  },
  "required": ["app"]
}
