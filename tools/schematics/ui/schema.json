{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsUi",
  "title": "UI Schematic",
  "type": "object",
  "description": "Generate a single UI component library within a domain",
  "properties": {
    "app": {
      "type": "string",
      "description": "Target app name (e.g., demo)",
      "x-prompt": "What is the target app name?"
    },
    "domain": {
      "type": "string",
      "description": "Domain name (e.g., products, users)",
      "x-prompt": "What is the domain name?"
    },
    "name": {
      "type": "string",
      "description": "UI component name (e.g., hero, card, table)",
      "x-prompt": "What is the UI component name?"
    }
  },
  "required": ["app", "domain", "name"]
}
