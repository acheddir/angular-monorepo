# @defaultRequired=infer @defaultSensitive=false
# @generateTypes(lang=ts, path=env.d.ts)

# @required
APP_API_URL=https://localhost:5000
# @required @type=enum(development, staging, production)
APP_ENVIRONMENT=development

APP_AUTH_ISSUER=
APP_AUTH_CLIENTID=
